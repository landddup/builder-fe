import createSession from "./createSession";
import signUp from "./signUp";
import restorePassword from "./restorePassword";

const rules = { createSession, signUp, restorePassword };

export default rules;
